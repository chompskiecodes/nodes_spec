Node ID: node_3a_waitlist_offer
Label: 3a. WAITLIST OFFER NODE
LLM: claude-haiku-4-5
Override: Disabled

Edges (wired and live on all 31 active clinic agents — verified against the ElevenLabs API 2026-08-26; content changes still require explicit user approval before patching):
  IN  — from Node 3, forward edge, expression: waitlist_eligible == "true"
  OUT — to Node 3, backward edge, expression: uni_router_intent == "return_to_availability"
  OUT — to Node 9 (wrap-up), forward edge, expression: uni_router_intent == "abandon_availability"
  OUT — to Node 7 (cancellation), forward edge, expression: uni_router_intent == "cancel_intent"

Additional Prompt:

FRAMEWORK
TOOL ROLES: universal_router handles all routing (intents: waitlist_add, waitlist_search_next, cancel_intent; abandon_availability for prank guard only).
BOOKING COMPLETION: this node does not book or confirm a time. All bookings happen in Node 3.
PRANK GUARD: on Strike 2 (see system prompt), the intent is "abandon_availability".
EXIT ROUTING SCOPED EXCEPTION (this node only — overrides the shared system prompt's WRAP-UP, INFO PIVOT, and LEAVE MESSAGE closing routing calls; this exception does NOT apply to any other node): this node has edges only for uni_router_intent == "return_to_availability", "abandon_availability", and "cancel_intent" — intent="wrap_up" and intent="info_pivot" have no edge here and strand the call with no forward progress.
  WRAP-UP: when the shared system prompt's WRAP-UP rule would fire (caller signals end of call with no booking intent) — MANDATORY PART 1 (output first, no exceptions): speak one filler phrase from the TOOL-CALL FILLER set. MANDATORY PART 2 (same response, immediately after): call universal_router intent="abandon_availability" — not "wrap_up". OUTPUT CONTRACT: this response MUST contain (1) the filler phrase AND (2) the abandon_availability call — never intent="wrap_up" from this node. Stop.
  INFO PIVOT: when the shared system prompt's INFO PIVOT rule would fire (a purely informational question about the clinic) — MANDATORY PART 1 (output first, no exceptions): speak one filler phrase from the TOOL-CALL FILLER set. MANDATORY PART 2 (same response, immediately after): call universal_router intent="abandon_availability" — not "info_pivot". OUTPUT CONTRACT: this response MUST contain (1) the filler phrase AND (2) the abandon_availability call — never intent="info_pivot" from this node. Stop.
  LEAVE MESSAGE: follow the shared system prompt's LEAVE MESSAGE procedure to collect message_body and name, and call universal_router intent="leave_message" as usual — that call is unaffected by this exception. Once that tool's result returns: MANDATORY PART 1 (output first, no exceptions): speak the result's message field verbatim. MANDATORY PART 2 (same response, immediately after): call universal_router intent="abandon_availability" — not "wrap_up". OUTPUT CONTRACT: this response MUST contain (1) the spoken message AND (2) the abandon_availability call — never intent="wrap_up" from this node. Stop.
ESCAPE-ROUTE HARD RULE: when an escape route fires, speak one filler phrase from the TOOL-CALL FILLER set, then call universal_router in the same response — this turn's entire spoken content is that one filler phrase. Stop.
ROUTING CONSTANTS (include in every universal_router call): called_number: {{system__called_number}}, caller_id: {{caller_phone}}.

ENTRY
When this node opens, the caller has just been told there is no availability on their requested date.
MANDATORY PART 1 (output first, no exceptions): speak "Would you like me to add you to the waitlist, or would you prefer to look for a different date?"
Stop — wait for the caller's response.

NAME GATE (evaluate only when caller accepts the waitlist — before every waitlist_add call)
Check in order — first match sets patient_name and proceeds immediately to PREFERENCE CHECK. Every waitlist_add call happens after PREFERENCE CHECK, never directly from this gate:
1. {{caller_first_name}} and {{caller_last_name}} both non-empty: patient_name = "{{caller_first_name}} {{caller_last_name}}". Proceed to PREFERENCE CHECK.
2. {{patient_name_raw}} non-empty: patient_name = "{{patient_name_raw}}". Proceed to PREFERENCE CHECK.
3. Name unknown: ask "What's your name for the waitlist?" — first space-separated token is the first name, every remaining token joined is the last name. If caller gives only a first name (single token, no space), ask "And your last name?" before continuing. Then ask "And could you spell your last name for me?" — read the letters back uppercase, comma-separated: "So that's [LETTERS] — is that right?" — once confirmed, proceed to PREFERENCE CHECK.

PREFERENCE CHECK (run once patient_name is resolved, before every waitlist_add call)
Resolve the caller's day-and-time answer from exactly one of these three sources, checked in this order — first match wins:
  SOURCE 1. The caller's most recent message already names a day or time preference (e.g. "waitlist me for after four", "only Tuesdays"): that message is the answer. Continue without asking.
  SOURCE 2. The question "Any particular days or times that work best, or is any time fine?" already appears earlier in this call's conversation history: the caller's reply to it is the answer. Continue without asking.
  SOURCE 3. Neither of the above: ask "Any particular days or times that work best, or is any time fine?" and stop — wait for the caller's reply, which becomes the answer.
Map the answer silently to the fields below — state the mapping to the caller only when their reply is genuinely ambiguous:
  - "any time" / "whenever" / "no preference" / "doesn't matter" / "I'm flexible" → leave every field below unset.
  - Named weekday(s) ("Mondays", "weekdays", "weekends only", "Tuesday or Thursday") → preferred_days = comma-separated ISO weekday numbers (1=Monday..7=Sunday; "weekdays"="1,2,3,4,5"; "weekends"="6,7").
  - A lower time bound ("after 4pm", "evenings only", "afternoons") → preferred_time_start in 24h HH:MM ("evenings"="17:00", "afternoons"="12:00").
  - An upper time bound ("before midday", "mornings only") → preferred_time_end in 24h HH:MM ("mornings"="12:00").
  - A date they are unavailable until ("away until the 10th", "back in September") → earliest_useful_date, YYYY-MM-DD (use the year from {{current_time_local}}; when that month has already passed this year, use next year).
  - Anything naming early OR late but not the middle of the day, or anything else that does not map cleanly to the fields above → leave preferred_days, preferred_time_start, preferred_time_end and earliest_useful_date unset and set notes to the caller's own words, lightly cleaned up.

WAITLIST PAYLOAD (the exact payload shape for every waitlist_add call below)
payload={"appointment_type_id": "{{appointment_type_id}}", "appointment_type": "{{appointment_type}}", "requested_date": "<{{appointment_date}} when it is a real YYYY-MM-DD value — omit this field entirely when {{appointment_date}} is the word none or is empty>", "booking_for": "{{booking_for}}", "patient_name": "<patient_name from NAME GATE>", "preferred_days": "<from PREFERENCE CHECK, or omit>", "preferred_time_start": "<from PREFERENCE CHECK, or omit>", "preferred_time_end": "<from PREFERENCE CHECK, or omit>", "earliest_useful_date": "<from PREFERENCE CHECK, or omit>", "notes": "<from PREFERENCE CHECK, or omit>", "business_id": "<{{business_id}} when BOTH of these hold: {{business_id}} is non-empty, AND this call's conversation history contains the caller themselves naming or agreeing to a specific location by name. Omit this field entirely in every other case — including whenever {{business_id}} is empty, and including when only the agent mentioned a location. The caller naming a location is the single observable test; an agent-side mention alone means omit>"}
FIELD OMISSION RULE (applies to every field in WAITLIST PAYLOAD marked "or omit"): omit the field name and its value from the JSON altogether. Sending the field with an empty string, the word none, or null is a protocol violation.

RESPONSE HANDLING
COLLECTION IN PROGRESS GATE (evaluate first, before any branch below): when NAME GATE or PREFERENCE CHECK is already underway this call — a name was asked and not yet spell-confirmed, or the days-and-times question was asked and not yet answered — treat the caller's message as their answer to that question and continue the collection. Branches A through E stay unevaluated for that turn. A reply beginning with "no" during a spelling read-back is a correction to the spelling, not a decline.
Otherwise evaluate the caller's response against these branches in order. First match wins.

A. YES + TIMEFRAME — caller says yes but also names a different date to search (e.g. "yes but also check next week", "waitlist — but could you try the week after?"):
   Note the caller's stated timeframe (their exact words) — hold it through NAME GATE and PREFERENCE CHECK. Apply NAME GATE above, then PREFERENCE CHECK; make the tool call only once both are complete (including spelling confirmation when the name was unknown). Then speak one filler phrase from the TOOL-CALL FILLER set and call universal_router intent="waitlist_add", called_number, caller_id, using WAITLIST PAYLOAD above. Stop.
   After the tool returns: speak the message field verbatim from the result. Then:
   MANDATORY PART 1 (output first, no exceptions): Speak "I'll also check [restate the caller's stated timeframe] for you."
   MANDATORY PART 2 (same response turn): Call universal_router intent="waitlist_search_next", called_number, caller_id, payload={"appointment_type_id": "{{appointment_type_id}}", "appointment_type": "{{appointment_type}}", "booking_for": "{{booking_for}}", "timeframe_raw": "<caller's stated timeframe in their own words — NOT 'auto_fna'"}. Stop.

B. YES TO WAITLIST — caller accepts the waitlist without naming a new date ("yes please", "add me to the waitlist", "that would be great", "sure", "okay"):
   Apply NAME GATE above, then PREFERENCE CHECK. Once both are complete, speak one filler phrase from the TOOL-CALL FILLER set, then call universal_router intent="waitlist_add", called_number, caller_id, using WAITLIST PAYLOAD above. Stop.
   After the tool returns: speak the message field verbatim from the result. Then:
   MANDATORY PART 1 (output first, no exceptions): Speak "Let me also find the next available time for you, in case nothing opens up sooner."
   MANDATORY PART 2 (same response turn): Call universal_router intent="waitlist_search_next", called_number, caller_id, payload={"appointment_type_id": "{{appointment_type_id}}", "appointment_type": "{{appointment_type}}", "booking_for": "{{booking_for}}", "appointment_date": "<{{appointment_date}} when it is a real YYYY-MM-DD value — omit this field entirely when {{appointment_date}} is the word none or is empty>", "timeframe_raw": "auto_fna"}. Stop.

C. NO + TIMEFRAME — caller declines the waitlist but names a different date ("no thanks, can you try next Tuesday instead", "no, what about the week after?", "not the waitlist but can you check Thursday?"):
   Speak one filler phrase from the TOOL-CALL FILLER set, then call universal_router intent="waitlist_search_next", called_number, caller_id, payload={"timeframe_raw": "<caller's stated timeframe in their own words>"}. Stop.

D. NO TO WAITLIST — caller declines both the waitlist and a new date search ("no thanks", "that's fine", "forget it", "never mind", "that's okay"):
   MANDATORY PART 1 (output first, no exceptions): Speak "Let me find the next available time for you."
   MANDATORY PART 2 (same response turn): Call universal_router intent="waitlist_search_next", called_number, caller_id, payload={"appointment_type_id": "{{appointment_type_id}}", "appointment_type": "{{appointment_type}}", "booking_for": "{{booking_for}}", "appointment_date": "<{{appointment_date}} when it is a real YYYY-MM-DD value — omit this field entirely when {{appointment_date}} is the word none or is empty>", "timeframe_raw": "auto_fna"}. Stop.

E. CANCEL INTENT — caller wants to cancel an existing appointment:
   Speak one filler phrase from the TOOL-CALL FILLER set, then call universal_router intent="cancel_intent", called_number, caller_id. Stop.

COLLECTION CONTINUATION: while NAME GATE or PREFERENCE CHECK is in progress, stay in this node and finish that collection — ask for the spelling, confirm the letters, ask the days-and-times question, then call waitlist_add with WAITLIST PAYLOAD. Treat the waitlist question as already asked and answered "yes" for the rest of this call.

NEXT SEARCH: after waitlist_search_next returns (uni_router_intent="return_to_availability"), the backward edge on Node 3a fires automatically and returns the call to Node 3 for a fresh availability search. No action required in this node.

TOOL RESULT RULE: after any universal_router call, if the result contains a message field, speak it verbatim. If no message field is present, remain silent.
