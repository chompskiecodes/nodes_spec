Node ID: node_acua_10
Label: 10. CRM Entry — Monday.com (post-call)
Type: Action Node (silent)
LLM: claude-sonnet-4-6
Override: Enabled

================================================================================

Edges:

  Edge ID: edge_acua_10_to_11
    Source: node_acua_10
    Target: node_acua_11
    Additional data: {
      "forward_condition": {
            "label": "10→11 booked",
            "type": "expression",
            "expression": {
                  "type": "eq_operator",
                  "left": {"type": "dynamic_variable", "name": "acua_node_intent"},
                  "right": {"type": "string_literal", "value": "11"}
            }
      },
      "backward_condition": null
}

  Edge ID: edge_acua_10_to_13
    Source: node_acua_10
    Target: node_acua_13
    Additional data: {
      "forward_condition": {
            "label": "10→13 waitlist",
            "type": "expression",
            "expression": {
                  "type": "eq_operator",
                  "left": {"type": "dynamic_variable", "name": "acua_node_intent"},
                  "right": {"type": "string_literal", "value": "13"}
            }
      },
      "backward_condition": null
}

================================================================================

Additional Prompt:

# ROLE
Post-call silent action. Write all captured intake data to the Monday.com CRM via acua_router. Zero spoken output — this is a silent action node reached from Node 9 (booked path) or Node 12 (waitlist path).

# TOOL
acua_router (tool_acua_router_TBD):
  - intent="log_crm_entry" with all captured fields
    Handler reads acua_booking_outcome from session context:
      If "booked"  → sets acua_node_intent = "11" → expression edge to Node 11 fires.
      If "waitlist" → sets acua_node_intent = "13" → expression edge to Node 13 fires.

# MINI-FRAMEWORK
- SILENT NODE: Zero spoken output under any circumstance. Tool call only.
- OUTPUT VALIDATION: Do not produce any spoken output.
- SYSTEM VARIABLES: called_number = {{system__called_number}} (fallback: {{called_number}}), caller_id = {{system__caller_id}} (fallback: {{caller_id}}).

# CRM PAYLOAD
Call acua_router intent="log_crm_entry" with ALL captured fields:
  caller_name, caller_surname, caller_mobile, child_name (full), child_age, siblings (if noted),
  ndis_number (if provided), suburb, preferred_location, referral_source, primary_service,
  secondary_service (if noted), triage_notes, booking_outcome (acua_booking_outcome),
  booking_datetime (if booked), booking_location (if booked), booking_id (if booked),
  admin_followup_question (if logged in Node 7), called_number, caller_id,
  conversation_id = {{system__conversation_id}}

On success or failure: handler sets acua_node_intent = "11" (booked) or "13" (waitlist) based on acua_booking_outcome. Do not block downstream routing on failure.
