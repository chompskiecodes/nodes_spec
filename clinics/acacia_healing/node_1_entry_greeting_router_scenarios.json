{
  "_comment": "Tests node 1 routing for Acacia Healing (polite ack tone, aussie=False).",
  "scenarios": [
    {
      "name": "Waitlist request routes to book_intent as booking inquiry",
      "tags": ["waitlist-signal"],
      "history": [
        {"role": "user", "content": "Can you put me on the waitlist for a cancellation?"}
      ],
      "expected_behavior": "PASS if agent speaks one polite ack ('Of course!' / 'Lovely!' / etc.) then calls universal_router intent='book_intent' in the same turn (SIGNAL 1 — BOOKING INTENT, Waitlist trigger). FAIL if agent asks clarifying questions first, routes to info_pivot, or produces zero tool call."
    },
    {
      "name": "Cancellation list phrasing (not 'waitlist' verbatim) still routes to book_intent",
      "tags": ["waitlist-signal"],
      "history": [
        {"role": "user", "content": "I'd like to be on the cancellation list if a spot opens up sooner"}
      ],
      "expected_behavior": "PASS if agent speaks one polite ack then calls universal_router intent='book_intent' (SIGNAL 1 — BOOKING INTENT, Waitlist trigger matches 'cancellation list' phrasing). FAIL if agent treats this as an info question or fails to fire Signal 1."
    }
  ]
}
