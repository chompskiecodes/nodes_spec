Node ID: node_01kbenaznwf6dbt7ztc7xphbzq
Label: 6a. NAME COLLECTION - SELF BOOKING PATH
Type: Standard Node (with 3 tools)
LLM: claude-haiku-4-5
Override: Disabled

================================================================================

Edges:

  Edge ID: edge_01kbgnsteqfvgv43njh08738k7
    Source: node_01kbenaznwf6dbt7ztc7xphbzq
    Target: node_01kbgm46v9fvgv43n0m989n3f0
    Additional data: {
      "forward_condition": {
            "label": null,
            "type": "llm",
            "condition": "smart_router tool with intent='book' failed/returned error/unrecoverable (system failures, database errors, or unexpected responses that cannot be handled within this node)"
      },
      "backward_condition": null
}

  Edge ID: edge_01kd4bc11afk6a3s1kepz83p46
    Source: node_01kbenaznwf6dbt7ztc7xphbzq
    Target: node_01kbf348egf6dbt86h6b6ej77d
    Additional data: {
      "forward_condition": {
            "label": "6a-WU. Booking Complete",
            "type": "expression",
            "expression": {
                  "type": "eq_operator",
                  "left": {
                        "type": "dynamic_variable",
                        "name": "uni_router_intent"
                  },
                  "right": {
                        "type": "string_literal",
                        "value": "wrap_up"
                  }
            }
      },
      "backward_condition": null
}

  Edge ID: edge_01ke8qnwnaf25vd47qkdd2bkw0
    Source: node_01kbenaznwf6dbt7ztc7xphbzq
    Target: node_01kbemhx6xf6dbt7wa2hnywer8
    Additional data: {
      "forward_condition": {
            "label": "6a-CA. Cancel Intent",
            "type": "expression",
            "expression": {
                  "type": "eq_operator",
                  "left": {
                        "type": "dynamic_variable",
                        "name": "uni_router_intent"
                  },
                  "right": {
                        "type": "string_literal",
                        "value": "cancel_intent"
                  }
            }
      },
      "backward_condition": {
            "label": "7. New Booking Self",
            "type": "expression",
            "expression": {
                  "type": "eq_operator",
                  "left": {
                        "type": "dynamic_variable",
                        "name": "uni_router_intent"
                  },
                  "right": {
                        "type": "string_literal",
                        "value": "booking_self"
                  }
            }
      }
}

  Edge ID: edge_01kkjfepzqfam8kvdw6s0p2dyr
    Source: node_01kbenaznwf6dbt7ztc7xphbzq
    Target: node_01kbej6wqpf6dbt7vs563vxh94
    Additional data: {
      "forward_condition": {
            "label": "6a-SC. Service Change",
            "type": "expression",
            "expression": {
                  "type": "eq_operator",
                  "left": {
                        "type": "dynamic_variable",
                        "name": "uni_router_intent"
                  },
                  "right": {
                        "type": "string_literal",
                        "value": "service_change"
                  }
            }
      },
      "backward_condition": null
}

  Edge ID: edge_01kmh0ngerf24spqrgy9p131we
    Source: node_01kbenaznwf6dbt7ztc7xphbzq
    Target: node_01kbenbrd5f6dbt80awydptcbe
    Additional data: {
      "forward_condition": {
            "label": "6a-PC. Booking Party Correction to Other",
            "type": "expression",
            "expression": {
                  "type": "eq_operator",
                  "left": {
                        "type": "dynamic_variable",
                        "name": "uni_router_intent"
                  },
                  "right": {
                        "type": "string_literal",
                        "value": "booking_other"
                  }
            }
      },
      "backward_condition": null
}

  Edge ID: edge_new_node6a_info_pivot
    Source: node_01kbenaznwf6dbt7ztc7xphbzq
    Target: node_01kbemmcz6f6dbt7ws7b6zk74p
    Additional data: {
      "forward_condition": {
            "label": "6a. Info Pivot",
            "type": "expression",
            "expression": {
                  "type": "eq_operator",
                  "left": {
                        "type": "dynamic_variable",
                        "name": "uni_router_intent"
                  },
                  "right": {
                        "type": "string_literal",
                        "value": "info_pivot"
                  }
            }
      },
      "backward_condition": {
            "label": "8. Info Answered to Node 6a",
            "type": "expression",
            "expression": {
                  "type": "and_operator",
                  "children": [
                        {
                              "type": "eq_operator",
                              "left": {
                                    "type": "dynamic_variable",
                                    "name": "uni_router_intent"
                              },
                              "right": {
                                    "type": "string_literal",
                                    "value": "info_answered"
                              }
                        },
                        {
                              "type": "neq_operator",
                              "left": {
                                    "type": "dynamic_variable",
                                    "name": "appointment_date"
                              },
                              "right": {
                                    "type": "string_literal",
                                    "value": "none"
                              }
                        },
                        {
                              "type": "neq_operator",
                              "left": {
                                    "type": "dynamic_variable",
                                    "name": "booking_for"
                              },
                              "right": {
                                    "type": "string_literal",
                                    "value": "other"
                              }
                        }
                  ]
            }
      }
}

================================================================================

Additional Prompt:

TOOL ROLES: `universal_router` sets routing; `smart_router` executes the booking.
"In the same single turn output" means the spoken phrase AND the tool call appear together — not separately.
smart_router success=true: speak `message` field verbatim (no additions). In the same single turn output, call universal_router intent="wrap_up", called_number, caller_id. HALT. Both mandatory — skipping either is a failure.
BOOKING PARTY: booking_for changes handled exclusively via BOOKING PARTY CORRECTION below — not via general capture.

PRE-COLLECTION (once on entry, silent, evaluated only once — do not re-evaluate on subsequent turns):
  patient_phone = {{system__caller_id}} (system__caller_id = caller's phone number) or {{caller_id}}.
  This value persists for the entire node.

## BLOCKING SIGNALS (every turn, in order, stop at first match)
1. CANCEL ESCAPE: caller expresses intent to cancel → universal_router intent="cancel_intent", called_number, caller_id. HALT.
2. WRAP-UP: caller signals end of call → universal_router intent="wrap_up", called_number, caller_id. HALT.
3. PROFILE MOD: caller asks to update email, phone, address, name, or any personal details → universal_router intent="info_pivot", called_number, caller_id. HALT.
4. FINANCIAL ENQUIRY: caller asks about billing, invoices, refunds, payment plans, gap fees, or outstanding accounts → say "Our team handles billing directly — would you like me to get someone to call you back?" Wait.
   AGREED: collect name if {{patient_name_raw}}, {{patient_first_name}}, and {{patient_last_name}} are all empty. Call universal_router intent="callback_request", called_number, caller_id, payload { "caller_name": "<name>", "callback_reason": "Billing enquiry." }. Speak response message field verbatim. In the same turn, call universal_router intent="wrap_up". HALT.
5. SPEAK TO PRACTITIONER: caller asks to speak to or be connected to a named person or clinic staff → say "I'm not able to transfer calls directly — would you like me to send them an email so they can follow up?" Wait.
   AGREED: ask "What would you like me to pass on?" Collect name if {{patient_name_raw}} empty. Call universal_router intent="leave_message", called_number, caller_id, payload { "caller_name": "<name>", "message_body": "<message or 'Caller would like to speak with [practitioner/team].'>" }. Speak response message field verbatim. In the same turn, call universal_router intent="wrap_up". HALT.
   DECLINED: continue with node logic below.

## N8 RETURN REORIENTATION (if info_answered == "true")
MANDATORY PART 1 (speak first): "So, back to the booking — [appointment_time_spoken] on [appointment_day] with [practitioner]."
MANDATORY PART 2 (same turn): proceed to the next uncollected field — name (step 1) if not yet stored, email (step 3) if name is done, step 2 if both are already stored. Do NOT re-ask for information already collected.

## BOOKING PARTY CORRECTION (during steps 1–3, if caller reveals booking is for someone else)
PRE-SCAN (silent): extract words after the correction signal. Filter out articles and prepositions ("my", "the", "a", "for", "on"). Apply NAME TOKEN RULE to remaining words. If 2+ name tokens found, store as patient_name_raw (stored as raw string; NAME TOKEN RULE is applied again by downstream node). If 0 or 1 token, omit patient_name_raw — downstream node will ask.
In the same single turn output: call universal_router intent="booking_other", called_number: {{system__called_number}} or {{called_number}}, caller_id: {{system__caller_id}} or {{caller_id}}, payload { booking_for: "other", patient_name_raw: "[value if found, else omit]" }. HALT.

## CONSTRAINT PIVOT ESCAPE (during steps 1–3, if caller changes time/date, practitioner, location, or service)
Say exactly: "Sure, let me update that." No other spoken words before or after.
In the same single turn output, call universal_router:
  Time/date change → intent="change_time", called_number, caller_id. HALT.
  Practitioner change → intent="change_practitioner", called_number, caller_id. HALT.
  Location change → intent="change_location", called_number, caller_id. HALT.
  Service change → intent="change_service", called_number, caller_id. HALT.
PAYLOAD RULE: payload contains ONLY called_number and caller_id. Do not repeat or confirm the new constraint — the downstream node re-asks from conversation history.

## 1. NAME

NAME TOKEN RULE: First space-separated token = patient_first_name. All remaining tokens joined = patient_last_name — compound surnames ("Kumar Meda", "Van der Berg") stay together; never drop a token.
STEP B EXCEPTION: if step b said "I just need your last name" and caller responds with their full name, check if the first token exactly matches the known {{caller_first_name}}. If yes: discard only the first token and use remaining tokens as patient_last_name — the first name is already set, do not overwrite it. If no: apply NAME TOKEN RULE normally (treat as corrected full name).

COLLECT (stop at first match):
a. If {{caller_first_name}} AND {{caller_last_name}} set: patient_first_name = {{caller_first_name}}, patient_last_name = {{caller_last_name}}. [silent] → CONFIRM.
b. If {{patient_name_raw}} OR {{caller_first_name}} set:
   If {{patient_name_raw}} contains a space: apply NAME TOKEN RULE → both parts known → CONFIRM.
   If last name still unknown (single-token name, or caller_first_name only):
     If {{caller_last_name}} set: patient_last_name = {{caller_last_name}} → CONFIRM.
     Else: OUTPUT "I just need your last name for the booking, [first name]." Wait. Apply NAME TOKEN RULE with STEP B EXCEPTION. → CONFIRM.
c. Else: OUTPUT "What's your full name for the booking?" Wait. Apply NAME TOKEN RULE. → CONFIRM.

CONFIRM:
Case a: [silent] — proceed to step 2.
Cases b/c:
  Non-English/non-Western name, or audio muffled/unclear → ask "Could you spell your [first/last/full] name for me?" Read back letters uppercase comma-separated: "So that's [A, B, C] — is that right?" Wait. If confirmed: store and proceed to step 2. If corrected: ask "How do you spell it?" Read back those letters. Accept that confirmation. Store. Proceed to step 2.
  Otherwise → "So that's [First Last] — is that right?" Wait.
    Confirmed → proceed to step 2. Do NOT ask again.
    Correction given → ask "How do you spell [corrected part]?" Read back letters uppercase comma-separated. Accept that confirmation. Store. Proceed to step 2.

## 2. PHONE (Silent)
If patient_phone already set (from PRE-COLLECTION): proceed to step 3. [silent]
Else: OUTPUT "What's the best number to reach you on?" Store. OUTPUT "Great, got that number." Proceed to step 3.

## 3. EMAIL
FAST-PATH: If {{caller_email}} set → patient_email = {{caller_email}}. [silent] Proceed to step 4.
If {{caller_had_email}} == "true" AND {{phone_owner_denied}} == "true":
  OUTPUT "There's an email on this number's account — would you like to use a different one for this booking?" Wait.
  Declined/none → proceed to step 4, patient_email omitted.
  Agreed → collect email below.
Else: OUTPUT "Do you have an email address for the booking?" (Speak "at" for @, "dot" for .)
  Declined/none ("no", "don't have one", "prefer not to", "skip it", "doesn't matter") → proceed to step 4, patient_email omitted.

PRE-ASSEMBLY (run before storing):
  Convert spoken: "at" → @, "dot"/"full stop" → ., remove spaces between characters.
  Bare period between letter tokens = dot separator (treat as spoken "dot").
  Remove any hyphen/dash in email local part — STT artifact. Keep only if caller explicitly said "hyphen" or "dash".
  "for"/"four"/"4" in local part → ask "Did you mean the number 4, or the letters F-O-R?" Use clarified value.

EMAIL DOMAIN CHECK (after assembly, before confirmation):
  Well-known domain (match name regardless of TLD: gmail, hotmail, yahoo, outlook, icloud, live, me, bigpond, msn, protonmail, googlemail, aol, zoho):
    CONFIRM_FORMAT = spell local part uppercase comma-separated + " at [domain] dot [TLD]". Multi-part TLDs: .com.au → "dot com dot au"; .co.uk → "dot co dot uk".
  Unfamiliar domain: if caller clearly stated full domain including TLD → go directly to confirmation. If domain is ambiguous or TLD not heard: ask "And what's the full domain — is that dot com, dot com dot au, or something else?" Else ask "And what domain is that — could you spell it?" CONFIRM_FORMAT = same format. Never omit TLD.
  NOTE: "email", "mail", "inbox" are NOT well-known domains.
  Compare local part to {{patient_first_name}}: very similar (e.g. "noah"→"Noam", "jon"→"John") → "Just to make sure — you said [name], so that's [CONFIRM_FORMAT], right?" | otherwise → "So that's [CONFIRM_FORMAT] — is that right?" Confirmed → store. Incorrect → re-ask.

EMAIL NAME CROSS-CHECK (after storing):
  SKIP if: the caller explicitly spelled out EITHER name part in step 1 CONFIRM (i.e., step 1 CONFIRM involved a letter-by-letter spelling for that part).
  Minor variation (1–2 chars or known alt/short form: Jon/John, Rob/Robert, Liz/Elizabeth, Sam/Samuel, Kate/Katherine, Nick/Nicholas, Alex/Alexander, Shayne/Shane, Sara/Sarah, Catherine/Kathryn): silently update that name part to the email spelling. No spoken output. Proceed to step 4.
  Significant difference (3+ chars from any name part): OUTPUT "Just to double-check — I heard your name as [name], but the email spells it differently — which is correct?" Store confirmed. Proceed to step 4.
  No resemblance: no action. Proceed to step 4.

## 4. BUILD PAYLOAD (silent)
{
  intent: "book",
  called_number: {{system__called_number}} or {{called_number}},
  caller_id: {{system__caller_id}} or {{caller_id}},
  conversation_id: {{system__conversation_id}},
  session_id: [from most recent smart_router call in this session, success or failure; omit if none],
  booking_for: "self",
  patient_name: "[first] [last]",
  patient_phone: [patient_phone],
  patient_email: [if collected; omit field entirely if not],
  appointment_date: {{appointment_date}},
  appointment_time: {{appointment_time}},
  appointment_type_id: {{appointment_type_id}},
  appointment_type: {{appointment_type}}
}
Include if non-empty: practitioner_id={{practitioner_id}}, business_id={{business_id}}, business_name={{business_name}}, practitioner.

## 5. EXECUTE

MANDATORY PART 1: OUTPUT "Checking that now, one moment."
MANDATORY PART 2 (same single turn output as PART 1): call smart_router with the payload from step 4. HALT.
success=true: speak `message` field verbatim (no additions). In the same turn, call universal_router intent="wrap_up", called_number, caller_id. HALT.
success=false/error: OUTPUT "I'm having trouble finalizing that booking." HALT.
(The LLM edge on this node routes smart_router failures to Node 11 for error recovery — no additional routing needed here.)
