Node ID: node_4_appointment_lookup
Label: 4. Appointment Lookup
LLM: claude-haiku-4-5
Override: Disabled

Edges (Source/Target ids as resolved by each injector at patch time):
  [add_node4_appointment_lookup.py — EL-API edges, source/target found by label prefix, no file glob]
  node_8_* (label prefix "8.")  <> node_4_appointment_lookup : forward uni_router_intent == "details" | backward uni_router_intent == "info_pivot"
  node_4_appointment_lookup -> node_1c_appointment_lookup_recovery (label prefix "1c.") : appointment_lookup_failed == "true"
  node_4_appointment_lookup -> node_7_* (label prefix "7.") : uni_router_intent == "cancel_intent" OR uni_router_intent == "reschedule"
  node_4_appointment_lookup -> node_9_* (label prefix "9.") : uni_router_intent == "wrap_up"

Additional Prompt:

FRAMEWORK
PURPOSE: this node exists only to deliver a phone-based appointment lookup that another node already triggered (universal_router intent="details") and hand the caller back out. It adds no behaviour beyond what the shared system prompt already provides on every Override: Disabled node — PATIENT APPOINTMENT LOOKUP (the lookup result, its NOT-FOUND CHECK, and details_ack), CANCEL ESCAPE, INFO PIVOT, and WRAP-UP — all inherited automatically. This node never asks the caller for a name, phone number, or date of birth itself: a not-found result routes automatically, via the appointment_lookup_failed expression edge, straight to the appointment-lookup-recovery node.
ROUTING CONSTANTS (include in every universal_router call): called_number: {{system__called_number}}, caller_id: {{caller_phone}}.
